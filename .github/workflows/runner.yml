name: Actions Runner Controller Demo

on:
  push:
    branches:
      - "develop"
  pull_request:
    types: [labeled, synchronize, reopened]
    branches:
    - 'develop'

  # Allow workflow to be dispatched on demand
  workflow_dispatch:

jobs:
  run-notebooks:
    if: ${{ contains(github.event.pull_request.labels.*.name, 'ci:approved-to-run') }}
    runs-on: arc-runner-set
    steps:
    - run: echo "🎉 This job uses runner scale set runners!"

  do-not-run-notebooks:
    if: ${{ ! contains(github.event.pull_request.labels.*.name, 'ci:approved-to-run') }}
    runs-on: ubuntu-latest
    steps:
    - run: echo "⚠️ Skipped. Add the label 'ci:approved-to-run' to run this job."